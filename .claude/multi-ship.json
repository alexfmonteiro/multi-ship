{
  "build_workflow": "mixed-model-burst",
  "spec_glob": "docs/specs/*.md",
  "verify": "gh pr checks $PR --watch",
  "notify": "echo NOTIFY:",
  "pr_body_convention": "multi-ship smoke test",
  "complete_cmd": "Reply with exactly the token: bookkeeping-done",
  "test_cmd": "PYTHONPATH=src pytest -q",
  "build_invariants": "stdlib-only Python; keep the pytest suite green; no secrets in code. This is a DOC-ONLY change: do not modify any file under src/ or tests/.",
  "smoke_instructions": "run `PYTHONPATH=src pytest -q` and confirm it is still green",
  "roles": {
    "scout": "haiku", "reader": "haiku", "planner": "opus",
    "judges": ["opus", "sonnet", "haiku"],
    "coder": {"hard": "opus", "routine": "sonnet"}, "verifier": "opus"
  }
}
